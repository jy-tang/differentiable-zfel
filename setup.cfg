[versioneer]
VCS = git
style = pep440
versionfile_source = zfel/_version.py
versionfile_build = zfel/_version.py
tag_prefix = v

[tool:pytest]
markers =
    slow: marks tests that exercise the heavy JAX/FEL optimization path
